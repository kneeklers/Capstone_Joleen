# Pi 5 web app: live stream + TFLite defect detection
flask>=2.3.0
opencv-python-headless>=4.8.0
numpy>=1.20.0
# Use tensorflow (includes TFLite). tflite-runtime has no wheel on Pi / some platforms.
tensorflow>=2.13
