# Training runtime (Colab / Kaggle / local GPU). Pi5 uses requirements.txt for inference only.
tensorflow>=2.14,<2.17
Pillow>=10.0.0
numpy>=1.24.0

# YOLOv8 (object detection: 6 defect types, multiple defects per image)
ultralytics>=8.0.0
